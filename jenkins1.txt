node {
stage (‘Prepare environment’) {
 git url: 'https://github.com/sree150/Vedika-UI.git'
 }
    stage('nodejs Build'){
    def nodejsHome=tool name: 'nodejs', type: 'nodejs'
    def nodejsCMD="${nodejsHome}/bin/nodejs"
   }
  sh ‘npm install -g @angular/cli’
  stage (‘Build’) {
  sh ‘npm install’
  sh ‘ng build’
  }
}


