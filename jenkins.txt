node{
   stage('SCM Checkout'){
       git credentialsId: 'git-creds', url: 'https://github.com/sree150/Vedika-UI.git'
   }
   stage('Install dependencies'){
     def nodejsHome = tool name: 'nodejs', type: 'nodejs'
   }
   stage('build){
     sh label: ' ', script: 'npm install'
     sh label: ' ', script:  'ng serve --host 0.0.0.0
    }
  }